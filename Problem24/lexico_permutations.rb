result = []

def permute(arr, m = "") 
    if(arr.length == 0)
        result.push(m)

    else
        for i in 0..arr.lenght-1
            curr = arr.slice()
            next = curr.splice(i,)
        end

    end
end